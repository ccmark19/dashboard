import RawLogin from './rawLogin';
import GraphContainer from '../../commonFunction/template/graphContainer';
const login = () => {
  return <GraphContainer graph={<RawLogin />} />;
};

export default login;
