import RawLaunch from './rawLaunch';
import GraphContainer from '../../commonFunction/template/graphContainer';
const launch = () => {
  return <GraphContainer graph={<RawLaunch />} />;
};

export default launch;
